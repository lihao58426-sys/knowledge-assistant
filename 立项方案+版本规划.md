# 个人知识库助手 — 立项方案 + 版本规划

> 项目代号：knowledge-assistant
> 创建日期：2026年7月12日
> 一句话：用自己的技术文档和项目代码做知识库，随时用手机问问题

---

## 一、项目定位

一个基于自己硬盘上所有 `.md` 文档和 `.py` 源码的 AI 问答系统。不是搜索引擎——是懂你项目的导师。

## 二、核心价值

```
企微做不到的：
  ❌ 读你本地 .py 源码
  ❌ 分析跨文件依赖
  ❌ 自动同步你改过的文件
  ❌ 生成对话后的待办清单

我们能做的：
  ✅ 所有项目源码 + 技术文档全部入库
  ✅ 改了文件自动更新索引
  ✅ 每个回答附带文档和代码的引用来源
```

## 三、各版本功能

### V1.0 — 基础问答（3 天）

```
功能：
  ✅ index.py    扫描文件夹 → .py + .md 切片 → ChromaDB
  ✅ server.py   FastAPI + 简单聊天界面
  ✅ 问答        问 → 检索 5 段 → DeepSeek → 回答
  ✅ 来源引用    回答底部标注参考了哪些文件

不做的：
  ❌ 文件角色表（V2.0）
  ❌ 对话历史（V2.0）
  ❌ 待办生成（V3.0）
```

### V2.0 — 结构化理解（+2 天）

```
新增：
  ✅ 文件角色表    每个 .py 标注：功能/上游/下游/解决的问题
  ✅ 跨文件问答    "改了 models.py 影响哪些文件"
  ✅ 对话历史     保留最近 10 轮对话上下文
```

### V3.0 — 智能助教（+2 天）

```
新增：
  ✅ 标注重点      你在文件里标记 [重点] → 检索时加权
  ✅ 自动待办      对话结束自动生成待验证事项 + 关联文件
  ✅ 角色表自动补全 新文件加入时，AI 自动建议角色
```

---

## 四、技术栈

```
后端：    Python 3.14 + FastAPI + Jinja2
向量库：  ChromaDB（pip install chromadb，零配置，类似 SQLite）
AI：      DeepSeek API（Chat Completions）
前端：    原生 HTML + CSS（手机适配，跟项目 5 一样）
部署：    Docker + 云服务器（阶段三）
```

## 五、逻辑图

```
┌──────────────────────────────────────────────┐
│             ① 索引构建（离线一次）              │
│                                              │
│  TO DO/*.md ──┐                              │
│  项目/*.py  ──┤→ index.py → 切段 → ChromaDB  │
│  (未来可加)   ─┘                              │
└──────────────────┬───────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────┐
│             ② 问答服务（在线）                  │
│                                              │
│  手机浏览器 → FastAPI :8002                   │
│       │                                      │
│       ▼                                      │
│  向量化问题 → ChromaDB 检索最相似 5 段         │
│       │                                      │
│       ▼                                      │
│  问题 + 检索结果 → DeepSeek API → 回答        │
│       │                                      │
│       ▼                                      │
│  回答 + 📁引用来源 → 手机浏览器显示            │
└──────────────────────────────────────────────┘
```

## 六、目录结构

```
knowledge-assistant/
├── server.py              FastAPI 服务器
├── rag_engine.py          ChromaDB 检索 + DeepSeek 问答
├── index.py               索引构建（读文件 → 切片 → 入库）
├── requirements.txt       Python 依赖
├── Dockerfile             容器化
├── chroma_db/             ChromaDB 数据目录（.gitignore）
├── templates/
│   └── chat.html          聊天界面（手机适配）
└── 立项方案+版本规划.md    本文档
```

## 七、跟现有项目的关系

```
pos_daily_report/     ─┐
rfm_report/           ─┤
auto_video/           ─┤→ index.py 扫描 → ChromaDB
live_stream/          ─┤
内网培训系统demo/      ─┤
TO DO/                ─┘

knowledge-assistant/   ← 独立新项目，只读不写
```

## 八、开发顺序

```
Day 1:  index.py + rag_engine.py（索引构建 + 检索问答，命令行验证）
Day 2:  server.py + templates/chat.html（FastAPI + 手机聊天界面）
Day 3:  测试 + Dockerfile + 推 GitHub
```

---

> 📅 编制日期：2026年7月12日
> 📌 阶段三任务 #19-22 的具体实施方案
