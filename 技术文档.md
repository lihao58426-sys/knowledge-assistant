# 个人知识库助手 — 技术文档

> 版本：V3.2（阅读模式 + 预生成讲解 + 文档中心）
> 日期：2026年7月13日

## 一、项目概述

基于 ChromaDB + DeepSeek 的 RAG 知识库问答系统。扫描自己所有项目源码和技术文档，构建向量索引，通过自然语言提问获取基于真实代码和文档的回答。

## 二、技术栈

| 层面 | 技术 | 说明 |
|------|------|------|
| 向量库 | ChromaDB 1.5 | 多模型支持，统一目录，不同 collection |
| 中文模型 | text2vec-base-chinese V2 | 中文语义检索精准 |
| 英文模型 | all-MiniLM-L6-v2 V1 | 英文模型（对比用） |
| 问答 | DeepSeek Chat API | 检索结果 + 问题 → 生成回答 |
| 后端 | FastAPI + Jinja2 | Web 界面 |
| 预生成讲解 | DeepSeek API + JSON | 36 文件结构化讲解，离线读取 |
| 阅读模式 | vanilla JS + localStorage | 左右联动 + 笔记 + 追问 |
| 模型加载 | sentence-transformers | 通用 embedding 包装 |

## 三、三种模式

### 搜索模式（RAG）
用户提问 → ChromaDB 检索 → DeepSeek 回答。选项目可限定搜索范围。

### 导师模式
选项目 → 勾文件 → 全文发 DeepSeek 讲解。支持多文件批量发送。

### 文档模式
列出 TO DO 文件夹 27 篇知识文档，点开进入阅读模式。

## 四、阅读模式（V3.2 核心）

```
┌──────────────┬─────────────────────────┐
│ 代码/文档     │ 讲解面板                  │
│ (可滚动)      │ ├ 技术栈标签               │
│              │ ├ 目录（点击跳转代码行）      │
│              │ ├ 函数总览                 │
│              │ └ 逐行讲解（可记笔记）       │
│              ├─────────────────────────┤
│              │ 追问面板（选中行唤醒）        │
└──────────────┴─────────────────────────┘
```

- **预生成讲解：** `generate_explanation.py` 一次生成存 JSON，打开秒加载
- **行号精准：** 代码加 L行号 前缀发给 AI，不自己数
- **双向联动：** 点目录跳代码，点代码跳讲解
- **笔记系统：** 页内弹窗编辑、标签搜索、代码上下文、localStorage 持久化
- **追问面板：** 选中代码行 → 底部唤醒 → 回答追加显示

## 五、多模型架构

```
MODELS 注册表（加新模型只需 5 行）
  v1: 英文 all-MiniLM-L6-v2 → chroma_db/knowledge_base_v1
  v2: 中文 text2vec-base-chinese → chroma_db/knowledge_base_v2

python index.py           → V2 全量重建
python index.py --v1      → V1 全量重建
python index.py --incremental → 增量更新（只处理变动文件）
```

## 六、目录结构

```
knowledge-assistant/
├── index.py                  # 多模型索引（全量+增量）
├── rag_engine.py             # 问答引擎（对话历史+项目过滤+来源去重）
├── server.py                 # Web 服务（搜索/导师/文档/阅读/逐行讲解）
├── generate_explanation.py   # 预生成讲解（调 DeepSeek → 存 JSON）
├── templates/
│   ├── chat.html             # 主页（搜索+导师+文档三模式）
│   ├── reader.html           # 阅读模式（左右联动+笔记+追问）
│   └── tutor.html            # 多文件导师
├── .explanations/            # 36 个预生成讲解 JSON
├── chroma_db/                # 向量库（V1+V2）
├── 立项方案+版本规划.md
├── 实验报告-RAG知识库V1-V2.md
├── 技术文档.md
└── v1-archive/               # V1 原始代码
```

## 七、使用方式

```bash
# 搜索问答
python server.py → http://localhost:8002 → 搜索模式

# 预生成讲解
python generate_explanation.py "文件路径"

# 增量索引
python index.py --incremental
```

## 八、开发踩坑（V3.2 新增）

| 问题 | 原因 | 解决 |
|------|------|------|
| 行号不准 | AI 自己数行号 | 代码加 L行号 前缀发给 AI |
| 同名文件覆盖 | config.py 存多个项目 | 文件名加项目前缀 |
| 测试文件漏掉 | 只扫了项目根目录 | tests/ 往上再取一层父目录 |
| `</script>` 重复 | 模板编辑多了一个闭合标签 | 全局搜索删除 |
| 旧笔记格式炸 JS | V3.1 存对象，V3.2 期望数组 | loadNotes() 兼容处理 |
| 端口冲突 | 旧进程没杀干净 | netstat + taskkill |
