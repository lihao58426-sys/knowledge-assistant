# 个人知识库助手 — 技术文档

> 版本：V2.0（多模型统一架构）
> 日期：2026年7月12日

## 一、项目概述

基于 ChromaDB + DeepSeek 的 RAG 知识库问答系统。扫描自己所有项目源码和技术文档，构建向量索引，通过自然语言提问获取基于真实代码和文档的回答。

## 二、技术栈

| 层面 | 技术 | 说明 |
|------|------|------|
| 向量库 | ChromaDB 1.5 | 多模型支持，统一目录，不同 collection |
| 中文模型 | text2vec-base-chinese V2 | 中文语义检索精准 |
| 英文模型 | all-MiniLM-L6-v2 V1 | 英文模型（对比用） |
| 问答 | DeepSeek Chat API | 检索结果 + 问题 → 生成回答 |
| 后端 | FastAPI + Jinja2 | Web 界面 |
| 模型加载 | sentence-transformers | 通用 embedding 包装 |

## 三、多模型架构

```
index.py / rag_engine.py（共用代码）
         │
    ┌────▼────┐
    │ MODELS  │  注册表（加新模型只需加 5 行）
    │  v1: 英文, chroma_db/knowledge_base_v1
    │  v2: 中文, chroma_db/knowledge_base_v2
    └────┬────┘
         │
    python index.py        → 构建 V2（默认中文）
    python index.py --v1   → 构建 V1（英文）
```

**切换模型 = 一行参数。** 扫描、切段、存库逻辑完全共用。

## 四、核心流程

```
构建索引（离线）：
  扫描 6 个文件夹 → 找到 .py + .md → 切片（800字/段）→ embedding 向量化 → ChromaDB

问答（在线）：
  用户提问 → 向量化 → ChromaDB 检索 Top 5 → 拼 prompt → DeepSeek → 回答 + 引用来源
```

## 五、目录结构

```
knowledge-assistant/
├── index.py               # 多模型索引构建（python index.py / --v1）
├── rag_engine.py          # 多模型问答引擎（python rag_engine.py "问题" / --v1）
├── server.py              # Web 界面（http://localhost:8002）
├── templates/
│   └── chat.html          # 手机适配聊天界面
├── chroma_db/             # 统一向量库（V1+V2 不同 collection）
├── 立项方案+版本规划.md
├── 实验报告-RAG知识库V1.md
├── 技术文档.md             # 本文档
└── v1-archive/            # V1 历史代码（server.py + templates）
```

## 六、使用方式

```bash
# 构建索引
python index.py             # V2 中文（默认）
python index.py --v1        # V1 英文

# 命令行问答
python rag_engine.py "多店支持怎么实现"
python rag_engine.py --v1 "crawler.py 反检测"   # 用 V1 对比

# Web 服务
python server.py
# → 浏览器 http://localhost:8002
```

## 七、实验结论

| | V1（英文模型） | V2（中文模型） |
|------|:--:|:--:|
| "多店支持怎么实现" | ❌ 完全不相关 | ✅ 四文件全分析 |
| "介绍知识库" | ✅ 准确 | ✅ 准确 |
| 检索质量 | 中文不行 | 大幅提升 |

**详见** [实验报告-RAG知识库V1.md](实验报告-RAG知识库V1.md)

## 八、开发踩坑

| 问题 | 原因 | 解决 |
|------|------|------|
| 模型下载反复超时 | HuggingFace 国内直连慢 | 换 VPN 节点多次重试 |
| 模型默认存 C 盘 | ChromaDB 硬编码缓存路径 | 接受 79MB，后续用 mklink |
| V1/V2 代码重复 | 每版本复制一份 | 多模型统一架构，MODELS 注册表 |
| chroma_db_v2 目录冗余 | 旧代码路径不统一 | 合并到 chroma_db/，collection 区分 |
| `HF_HOME` 不生效 | ChromaDB 不认这个变量 | Windows mklink 符号链接 |
