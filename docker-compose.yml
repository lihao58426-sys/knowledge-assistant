# ── knowledge-assistant 服务编排 ──
# 用法：
#   docker compose up -d          # 后台启动
#   docker compose down           # 停止
#   docker compose logs -f        # 看日志

services:
  knowledge-assistant:
    build: .
    image: knowledge-assistant:latest
    container_name: knowledge-assistant
    restart: unless-stopped
    ports:
      - "8002:8002"
    environment:
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}       # 从宿主机环境变量传入，不进镜像
      - KA_WORKSPACE_ROOT=${KA_WORKSPACE_ROOT:-}   # 可选：覆盖工作区路径，默认不设
    volumes:
      - ./chroma_db:/app/chroma_db                 # 向量库持久化：容器删了数据还在
